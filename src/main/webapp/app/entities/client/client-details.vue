<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="client">
        <h2 class="jh-entity-heading" data-cy="clientDetailsHeading">
          <span v-text="$t('coopcycleApp.client.detail.title')">Client</span> {{ client.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="$t('coopcycleApp.client.idC')">Id C</span>
          </dt>
          <dd>
            <span>{{ client.idC }}</span>
          </dd>
          <dt>
            <span v-text="$t('coopcycleApp.client.nomC')">Nom C</span>
          </dt>
          <dd>
            <span>{{ client.nomC }}</span>
          </dd>
          <dt>
            <span v-text="$t('coopcycleApp.client.prenomC')">Prenom C</span>
          </dt>
          <dd>
            <span>{{ client.prenomC }}</span>
          </dd>
          <dt>
            <span v-text="$t('coopcycleApp.client.adresseC')">Adresse C</span>
          </dt>
          <dd>
            <span>{{ client.adresseC }}</span>
          </dd>
          <dt>
            <span v-text="$t('coopcycleApp.client.emailC')">Email C</span>
          </dt>
          <dd>
            <span>{{ client.emailC }}</span>
          </dd>
          <dt>
            <span v-text="$t('coopcycleApp.client.telephoneC')">Telephone C</span>
          </dt>
          <dd>
            <span>{{ client.telephoneC }}</span>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
        </button>
        <router-link v-if="client.id" :to="{ name: 'ClientEdit', params: { clientId: client.id } }" custom v-slot="{ navigate }">
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.edit')"> Edit</span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./client-details.component.ts"></script>
